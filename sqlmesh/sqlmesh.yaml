# SQLMesh Configuration for Gaming Analytics Pipeline

gateways:
  local:
    connection:
      type: duckdb
      database: /Users/mohamedboughattas/gaming-analytics-pipeline/data/gaming_analytics.duckdb

default_gateway: local

model_defaults:
  dialect: duckdb
