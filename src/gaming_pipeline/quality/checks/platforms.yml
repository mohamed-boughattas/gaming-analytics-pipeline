# Soda v4 contract for marts.platforms table
# Data quality checks for platforms mart dimension table

dataset: gaming_analytics/marts/platforms
columns:
  # Primary key validation
  - name: id
    checks:
      - name: Platform ID not null
        missing:
          score: 0
      - name: Platform ID unique
        duplicate:
          score: 0

  # Required business fields
  - name: name
    checks:
      - name: Platform name not null
        missing:
          score: 0
      - name: Platform name reasonable length
        length:
          min: 1
          max: 100

  # Slug field (URL-friendly identifier)
  - name: slug
    checks:
      - name: Slug not null
        missing:
          score: 0
      - name: Slug format valid (lowercase, hyphens, parentheses)
        invalid:
          threshold: 0.01
          regex: "^[a-z0-9]+(-[a-z0-9]+)*(\\([0-9]+\\))?$"

# Table-level checks
checks:
  - name: Platforms mart has data
    row_count:
      greater_than: 0

  - name: Reasonable platform count
    row_count:
      less_than: 200

  - name: All platforms have names
    missing_percent:
      column: name
      less_than: 0.1

  - name: All platforms have slugs
    missing_percent:
      column: slug
      less_than: 0.1
